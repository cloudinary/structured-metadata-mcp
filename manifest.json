{
  "author": {
    "name": "Cloudinary",
    "url": "https://cloudinary.com"
  },
  "description": "Cloudinary Structured Metadata MCP Server",
  "display_name": "Cloudinary Structured Metadata",
  "documentation": "https://cloudinary.com/documentation/cloudinary_llm_mcp",
  "homepage": "https://github.com/cloudinary/structured-metadata-mcp",
  "icon": "icon.png",
  "keywords": [
    "cloudinary",
    "structured-metadata",
    "metadata-fields",
    "metadata-rules",
    "asset-metadata"
  ],
  "license": "MIT",
  "long_description": "MCP server for managing Cloudinary structured metadata including creating, updating, and managing metadata fields and rules.",
  "manifest_version": "0.3",
  "name": "@cloudinary/structured-metadata-mcp",
  "privacy_policies": [
    "https://cloudinary.com/privacy"
  ],
  "prompts": [],
  "repository": {
      "type": "git",
      "url": "https://github.com/cloudinary/structured-metadata-mcp.git"
    },
  "screenshots": [
      "icon.png"
    ],
  "server": {
      "type": "node",
      "entry_point": "./bin/mcp-server.js",
      "mcp_config": {
        "command": "node",
        "args": [
          "${__dirname}/bin/mcp-server.js",
          "start",
          "--server-index",
          "${user_config.server_index}",
          "--region",
          "${user_config.region}",
          "--api-host",
          "${user_config.api_host}",
          "--api-key",
          "${user_config.api_key}",
          "--api-secret",
          "${user_config.api_secret}",
          "--oauth2",
          "${user_config.oauth2}",
          "--cloud-name",
          "${user_config.cloud_name}"
        ]
      }
    },
  "tools": [
      {
        "name": "create-metadata-field",
        "description": "Creates a new structured metadata field in your account\n\nCreates a new metadata field with the specified properties and configuration."
      },
      {
        "name": "list-metadata-fields",
        "description": "Lists all structured metadata fields defined in your Cloudinary product environment\n\nRetrieves a list of all metadata fields in the product environment based on the provided filters."
      },
      {
        "name": "get-metadata-field",
        "description": "Retrieves the definition of a specific structured metadata field by its identifier (external_id)\n\nRetrieves detailed information about the specified metadata field."
      },
      {
        "name": "update-metadata-field",
        "description": "Updates the configuration of an existing metadata field\n\nUpdates the properties and configuration of the specified metadata field."
      },
      {
        "name": "delete-metadata-field",
        "description": "Deletes a structured metadata field definition from your account\n\nPermanently deletes the specified metadata field and all its associated data."
      },
      {
        "name": "update-metadata-datasource-values",
        "description": "Updates the allowed values (the datasource) for a specified metadata field\n\nUpdates the values in a metadata field's datasource, including adding, modifying, or changing the order of values."
      },
      {
        "name": "delete-metadata-datasource-values",
        "description": "Removes one or more allowed values from a metadata field's datasource\n\nRemoves specific values from a metadata field's datasource by their external IDs."
      },
      {
        "name": "create-metadata-rule",
        "description": "Creates a new conditional metadata rule\n\nCreates a new metadata rule with the specified properties and configuration."
      },
      {
        "name": "list-metadata-rules",
        "description": "Retrieves a list of all conditional metadata rules defined in your accountcloudinary\n\nRetrieves a list of all metadata rules in the cloud."
      },
      {
        "name": "update-metadata-rule",
        "description": "Updates an existing conditional metadata rule's definition\n\nUpdates the properties and configuration of an existing metadata rule."
      },
      {
        "name": "delete-metadata-rule",
        "description": "Deletes a conditional metadata rule by its ID\n\nPermanently deletes a metadata rule."
      }
    ],
  "user_config": {
      "api_host": {
        "type": "string",
        "title": "host",
        "description": "API host domain. Templated in https://{host}.",
        "required": false,
        "default": "api.cloudinary.com"
      },
      "api_key": {
        "description": "Your Cloudinary API key. Found in Dashboard → Settings → Access Keys.",
        "required": true,
        "sensitive": true,
        "title": "API Key",
        "type": "string"
      },
      "api_secret": {
        "description": "Your Cloudinary API secret. Found in Dashboard → Settings → Access Keys.",
        "required": true,
        "sensitive": true,
        "title": "API Secret",
        "type": "string"
      },
      "cloud_name": {
        "description": "Your Cloudinary cloud name (required). Find it in your Cloudinary Dashboard.",
        "required": true,
        "title": "Cloud Name",
        "type": "string"
      },
      "oauth2": {
        "type": "string",
        "title": "oauth2",
        "description": "The oauth2 to use for the request",
        "required": false,
        "sensitive": true
      },
      "region": {
        "type": "string",
        "title": "region",
        "description": "Regional endpoint selection Templated in https://{region}.cloudinary.com. Options: [api, api-eu, api-ap].",
        "required": false,
        "default": "api"
      },
      "server_index": {
        "type": "number",
        "title": "Server Index",
        "description": "Available servers: [https://{region}.cloudinary.com, https://{host}]",
        "required": false,
        "default": "0",
        "max": 1
      }
    },
  "version": "0.3.1"
}